# Generated by Cargo
# will have compiled files and executables
/target/
/gen/schemas

# Autogenerated app-command ACL permissions (see build.rs's `APP_COMMANDS`)
# — fully derived from that list on every build, same "generated is not
# source" treatment as /gen/schemas above.
/permissions/autogenerated

# Staged by scripts/stage-frameworks.sh from target/<profile>/ before
# `tauri build` bundles them into Contents/Frameworks — machine-specific
# build output, not source.
/frameworks/
